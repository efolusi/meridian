{
  "name": "pagecontrol",
  "type": "registry:ui",
  "title": "PageControl",
  "description": "Carousel dots \u2014 active dot stretches to an ink pill.",
  "files": [
    {
      "path": "apps/www/registry/ui/navigation/PageControl.jsx",
      "type": "registry:ui",
      "content": "import React from 'react';\nimport { injectEfCss } from '../forms/Button.jsx';\nconst CSS = `\n.ef-dots{display:inline-flex;align-items:center;gap:6px}\n.ef-dots__dot{width:7px;height:7px;padding:0;border:none;border-radius:var(--radius-full);background:var(--border-strong);cursor:pointer;transition:background var(--dur-fast) var(--ease-out),width var(--dur-med) var(--ease-out)}\n.ef-dots__dot:hover{background:var(--sand-400)}\n.ef-dots__dot--on{width:20px;background:var(--accent)}\n.ef-dots__dot:focus-visible{outline:none;box-shadow:var(--focus-ring)}\n`;\nexport function PageControl({ count, index = 0, onChange, label = 'Page', style, className }) {\n  injectEfCss('ef-css-dots', CSS);\n  return (\n    <div role=\"tablist\" aria-label={label} className={`ef-dots${className ? ' ' + className : ''}`} style={style}>\n      {Array.from({ length: count }, (_, i) => (\n        <button key={i} role=\"tab\" aria-selected={i === index} aria-label={`${label} ${i + 1} of ${count}`}\n          className={`ef-dots__dot${i === index ? ' ef-dots__dot--on' : ''}`} onClick={() => onChange && onChange(i)} />\n      ))}\n    </div>\n  );\n}\n"
    },
    {
      "path": "apps/www/registry/ui/navigation/PageControl.d.ts",
      "type": "registry:component",
      "content": "export interface PageControlProps {\n  count: number;\n  /** Active index, 0-based @default 0 */\n  index?: number;\n  onChange?: (index: number) => void;\n  /** aria-label prefix @default 'Page' */\n  label?: string;\n  style?: React.CSSProperties;\n  className?: string;\n}\nexport declare function PageControl(props: PageControlProps): JSX.Element;\n"
    },
    {
      "path": "apps/www/registry/ui/navigation/PageControl.prompt.md",
      "type": "registry:component",
      "content": "Carousel dots \u2014 active dot stretches to an ink pill.\n\n```jsx\n<PageControl count={4} index={i} onChange={setI} />\n```\n"
    }
  ],
  "categories": [
    "navigation"
  ],
  "$schema": "https://ui.shadcn.com/schema/registry-item.json"
}
