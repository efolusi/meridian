{
  "name": "meridian-build",
  "version": "1.21.3",
  "private": true,
  "description": "Build tooling for the Meridian design system. Not the published package — consumers use the CDN or vendor the folder; see README.md.",
  "type": "module",
  "workspaces": [
    "mcp",
    "packages/*"
  ],
  "scripts": {
    "build:bundle": "node scripts/build_bundle.mjs",
    "build:npm": "node scripts/build_npm.mjs",
    "check": "node scripts/check_all.mjs",
    "check:bundle": "node scripts/build_bundle.mjs --check",
    "check:fix": "node scripts/check_all.mjs --fix",
    "check:npm": "node scripts/build_npm.mjs --check",
    "check:types": "node scripts/check_types.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:visual": "node scripts/check_visual.mjs"
  },
  "devDependencies": {
    "@babel/standalone": "7.29.0",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "jsdom": "^25.0.1",
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^3.8.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
